<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing is eligible for cloud backup or device-to-device transfer. The
     encrypted ingest-token store stays on this device only. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
